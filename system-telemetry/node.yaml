name: system-telemetry
version: "0.2.0"
type: python
description: System metrics publisher (CPU, memory, disk, network, load)
author: Bubbaloop Team

command: pixi run main

capabilities:
  - sensor

publishes:
  - suffix: system-telemetry/metrics
    description: "System metrics — CPU, memory, disk, network I/O, load average"

commands:
  - name: set_rate
    description: "Change the publish rate"
    params:
      rate_hz: float            # 0.001–100.0

requires:
  hardware: []
  software:
    - Python 3.11+
    - psutil
