name: oak_primary

width: 1280
height: 720
fps: 30

jpeg_every_n: 3
jpeg_quality: 80

# IMU sample rate (Hz). Depth and IMU are always attempted; gracefully
# disabled when the hardware is absent (e.g. OAK-1 has no stereo or IMU).
imu_hz: 50
