nodes:
  - name: rtsp-camera
    description: "RTSP camera capture with hardware H264 decode"
    version: "0.1.0"
    type: rust
    category: camera
    tags: [sensor, video, rtsp, gstreamer, h264, camera, gpu-accelerated]
    repo: kornia/bubbaloop-nodes-official
    subdir: rtsp-camera
    binary: rtsp_camera_node

  - name: openmeteo
    description: "Open-Meteo weather data publisher"
    version: "0.2.0"
    type: python
    category: weather
    tags: [sensor, weather, api, forecast]
    repo: kornia/bubbaloop-nodes-official
    subdir: openmeteo

  - name: system-telemetry
    description: "System telemetry metrics (CPU, memory, disk, network, load)"
    version: "0.2.0"
    type: python
    category: telemetry
    tags: [sensor, system, cpu, memory, disk, network, monitoring]
    repo: kornia/bubbaloop-nodes-official
    subdir: system-telemetry

  - name: network-monitor
    description: "Network connectivity monitor (HTTP, DNS, ping health checks)"
    version: "0.2.0"
    type: python
    category: monitoring
    tags: [sensor, network, dns, ping, http, connectivity]
    repo: kornia/bubbaloop-nodes-official
    subdir: network-monitor

  - name: camera-object-detector
    description: "Object detection using YOLO11 on camera raw frames"
    version: "0.2.0"
    type: python
    category: vision
    tags: [processor, detection, yolo, gpu]
    repo: kornia/bubbaloop-nodes-official
    subdir: camera-object-detector

  - name: camera-vlm
    description: "Vision-language model inference on camera frames"
    version: "0.1.0"
    type: python
    category: vision
    tags: [processor, vlm, ollama, gpu]
    repo: kornia/bubbaloop-nodes-official
    subdir: camera-vlm
